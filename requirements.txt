# Solar Forecasting Project — Python Dependencies
# Tested with Python 3.10+ (3.11 recommended)
# Versions recorded from the development environment (conda env: ai).

# Core data & ML
pandas>=2.0,==2.3.3
numpy>=1.24,==2.3.3
scikit-learn>=1.6,==1.7.2
joblib>=1.0,==1.5.2

# Deep learning  (PyTorch — install SEPARATELY; see note below)
# torch>=2.0

# Statistical modelling
statsmodels>=0.14,==0.14.6

# Solar physics
pvlib>=0.9,==0.15.2

# Hyperparameter optimisation
optuna>=3.0,==4.9.0

# Visualisation
matplotlib>=3.7,==3.10.7
plotly>=5.0,==6.5.0

# Web app
streamlit>=1.30,==1.52.1

# Utilities
PyYAML>=6.0,==6.0.3
tqdm>=4.60,==4.67.1

# ─────────────────────────────────────────────────────────────────────────────
# PyTorch Installation (REQUIRED — not pip-installable from the index above)
# Choose the command matching your hardware:
#
#   ROCm (AMD GPU):
#     pip install --no-cache-dir torch torchvision torchaudio \
#         --index-url https://download.pytorch.org/whl/rocm6.3
#
#   NVIDIA GPU (CUDA):
#     pip install --no-cache-dir torch torchvision torchaudio
#
#   CPU only:
#     pip install --no-cache-dir torch torchvision torchaudio \
#         --index-url https://download.pytorch.org/whl/cpu
#
# --no-cache-dir is recommended to avoid "Memoryview is too large" errors
# caused by PyTorch's large wheel files.
# ─────────────────────────────────────────────────────────────────────────────
